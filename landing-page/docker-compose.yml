version: '3'
services:
  noah-site:
    image: nginx:alpine
    container_name: noah-site
    ports:
      - "8082:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
