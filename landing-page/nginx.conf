server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Try to serve files without .html extension
    location / {
        try_files $uri $uri.html $uri/ =404;
    }
}
